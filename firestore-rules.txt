rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir leitura/escrita apenas para usuarios autenticados
    match /dados/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
